
    <div class="row">
        <div class="top-right col-sm-2">
            <a href="/" class="waves-effect waves-teal btn-flat">Log In</a>
        </div>
    </div>
    {{#if forgot}}
    <div class="row">
        <div class="main col-sm-4">
            <h1>Forgotten Password? We can help with that.</h1>
            <form id="reset" name="reset" method="post" action="/forgot">
                <div class="input-field">
                    <input id="email" type="email" class="validate">
                    <label for="email">Email</label>
                </div>
                <input class="waves-effect waves-light btn submit" type="submit" value="Reset Password">
                </a>
            </form>
        </div>
    </div>
    {{#if flashInfo}}
        <div>{{flashInfo}}</div>
    {{/if}}
    {{#if flashError}}
        <div>{{flashError}}</div>
    {{/if}}
{{/if}}

{{#if reset}}
    {{#unless flashError}}
        <div class="row">
            <div class="col-sm-4 main">
                <h1>Please Reset Your Password Now</h1>
                <form id="reset" name="reset" method="post" action="/reset/{{token}}">
                    <div class="input-field">
                        <input id="email" type="email" class="validate">
                        <label for="email">Email</label>
                    </div>
                    <div class="input-field">
                        <input id="pass" type="password" name="password" class="validate">
                        <label for="password">New Password</label>
                    </div>
                    <div class="input-field">
                        <input id="re-pass" type="password" class="validate">
                        <label for="re-pass">Confirm Password</label>
                    </div>
                    <input class="waves-effect waves-light btn submit" type="submit" value="Reset">
                    </a>
                </form>
            </div>
        </div>
    {{/unless}}
    {{#if flashError}}
        <div>{{flashError}}</div>
        <a href="/forgot" class="waves-effect waves-teal btn-flat">Try Resetting Password Again</a>
    {{/if}}
{{/if}}

{{#if signup}}
<div class="row">
    <div class="main col-sm-4">
        <h1>Sign-Up!</h1>
        <form id="signup" name="signup" method="post" action="/signup">
            <div class="input-field">
                <input id="email" type="email" class="validate">
                <label for="email">Email</label>
            </div>
            <div class="input-field">
                <input id="pass" type="password" class="validate">
                <label for="pass">Password</label>
            </div>
            <div class="input-field">
                <input id="re-pass" type="password" class="validate">
                <label for="re-pass">Confirm Password</label>
            </div>
            <input class="waves-effect waves-light btn submit" type="submit" value="Register">
            </a>
        </form>
        {{#if flashError}}
            <div>{{flashError}}</div>
        {{/if}}
    </div> 
</div>   
{{/if}}

<script src="/javascript/auth.js"></script>
