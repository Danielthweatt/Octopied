    <div class="row">
        <div class="top-right offset-s10 col s2">
            <a href="/" class="waves-effect waves-teal btn-flat">Log In</a>
        </div>
    </div>
    {{#if forgot}}
    <div class="row">
        <div class="main offset-s4 col s4">
            <h1>Forgotten Password? We can help with that.</h1>
            <form id="reset" name="reset" method="post" action="/forgot" novalidate>
                <div class="input-field">
                    <label for="email">Email address</label>
                    <input id="email" type="email" name="email" class="validate" required>
                    <span class="helper-text" data-error="Please enter a valid email address." data-success="Perfect"></span>
                </div>
                <input class="waves-effect waves-light" type="submit" value="Reset Password">
                </a>
            </form>
        </div>
    </div>
    {{#if flashInfo}}
        <div>{{flashInfo}}</div>
    {{/if}}
    {{#if flashError}}
        <div>{{flashError}}</div>
    {{/if}}
{{/if}}

{{#if reset}}
    {{#unless flashError}}
        <div class="row">
            <div class="offset-s4 col s4 main">
                <h1>Please Reset Your Password Now</h1>
                <form id="reset" name="reset" method="post" action="/reset/{{token}}" novalidate>
                    <div class="input-field">
                        <label for="email">Email address</label>
                        <input id="email" type="email" name="email" class="validate" required>
                        <span class="helper-text" data-error="Please enter a valid email address." data-success="Perfect"></span>
                    </div>
                    <div class="input-field">
                        <label for="password">Password</label>
                        <input id="password" type="password" minlength="5" name="password" class="validate" required>
                        <span class="helper-text" data-error="Password must be 5 characters" data-success="Perfect"></span>
                    </div>
                    <div class="input-field">
                        <label for="re-pass">Confirm Password</label>
                        <input id="re-pass" type="password" name="re-pass" equalTo="password" class="validate" required>
                         <span class="helper-text" data-error="Your passwords do not match" data-success="Perfect"></span>
                    </div>
                    <input class="waves-effect waves-light" type="submit" value="Reset">
                    </a>
                </form>
            </div>
        </div>
    {{/unless}}
    {{#if flashError}}
        <div>{{flashError}}</div>
        <a href="/forgot" class="waves-effect waves-teal btn-flat">Try Resetting Password Again</a>
    {{/if}}
{{/if}}

{{#if signup}}
<div class="row">
    <div class="main offset-s4 col s4">
        <h1>Sign-Up!</h1>
        <form id="signup" name="signup" method="post" action="/signup">
            <div class="input-field">
                <label for="email">Email address</label>
                <input id="email" type="email" name="email" class="validate" required>
                <span class="helper-text" data-error="Please enter a valid email address." data-success="Perfect"></span>
            </div>
            <div class="input-field">
                <label for="password">Password</label>
                <input id="password" type="password" minlength="5" name="password" class="validate" required>
                <span class="helper-text" data-error="Password must be 5 characters" data-success="Perfect"></span>
            </div>
                <div class="input-field">
                <label for="repass">Confirm Password</label>
                <input id="repass" type="password" name="repass" equalTo="#password" class="validate" required>
                <span class="helper-text" data-error="Your passwords do not match" data-success="Perfect"></span>
            </div>
            <input class="waves-effect waves-light btn" type="submit" value="Register"></a>
        </form>
        {{#if flashError}}
            <div>{{flashError}}</div>
        {{/if}}
    </div> 
</div>   
{{/if}}

<script src="/javascript/auth.js"></script>
<script>
$("#signin").validate({
    rules: {
        repass: {
        equalTo: "#password"
        }
    },
    errorElement : 'div',
    errorPlacement: function(error, element) {
        var placement = $(element).data('error');
        if (placement) {
            $(placement).append(error)
        } else {
            error.insertAfter(element);
        }
    }
});

</script>